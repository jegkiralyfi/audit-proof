# Heuristic provider

The heuristic provider is the offline fallback.
It uses configured pass/fail patterns and never calls a remote API.

Use it for:
- deterministic smoke tests
- offline demos
- baseline comparisons
