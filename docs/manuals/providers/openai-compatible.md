# OpenAI-compatible provider

Use this profile for any backend exposing an OpenAI-compatible Chat Completions API.

Typical config:
- provider: `openai-compatible`
- base URL: backend-specific `/v1` root
- API key env: backend-specific

Examples are documented separately for vLLM, TGI, and llama.cpp.
